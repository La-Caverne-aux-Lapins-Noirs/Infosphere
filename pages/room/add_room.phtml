<h2><?=$Dictionnary["AddARoom"]; ?></h2><br />
<form
    method="POST"
    action="/api/room"
    enctype="multipart/form-data"
    onsubmit="return false;"
>
    <input
	type="text"
	name="codename"
	placeholder="<?=$Dictionnary["CodeName"]; ?>"
	value="<?=try_get($_POST, "codename"); ?>"
    /><br />
    <input
	type="text"
	placeholder="<?=$Dictionnary["RoomCapacity"]; ?>"
	name="capacity"
	value=""
    /><br />
    <br />
    <h5><?=$Dictionnary["RoomMap"]; ?></h5>
    <input
	type="file"
	name="icon"
	accept="image/png"
	placeholder="<?=$Dictionnary["Icon"]; ?>"
	value="<?=try_get($_POST, "icon"); ?>"
    /><br />
    <br />
    <p class="room_form_hint">
        Les postes se placent ensuite à la souris depuis l’éditeur de salle.
    </p>
    <?php if (is_admin()) { ?>
        <select name="school">
            <option value="">École à rattacher plus tard</option>
            <?php foreach (fetch_school() as $school) { ?>
                <option value="<?=$school["id"]; ?>">
                    <?=htmlentities($school["codename"]); ?>
                </option>
            <?php } ?>
        </select><br />
        <small class="room_form_hint">L’administrateur peut aussi modifier le rattachement école depuis la salle.</small><br />
    <?php } ?>
    
    <?php forge_language_formular(["name" => "text"], $_POST); ?>
    <br />
    <br />

    <input
	type="button"
	onclick="silent_submitf(this.parentNode, {tofill: 'roomlist', toclear: 'roomlist', wrapper: 'rooms', clear_form: true});"
	value="&#10003;"
    />
</form>

